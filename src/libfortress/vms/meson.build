# SPDX-License-Identifier: BSD-3-Clause

library_hdrs_vms = files([

])

library_srcs += files([

])

if not meson.is_subproject()
	install_headers(
		library_hdrs_vms,
		subdir: 'libfortress' / 'vms'
	)
endif
