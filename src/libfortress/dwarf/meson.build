# SPDX-License-Identifier: BSD-3-Clause

library_hdrs_dwarf = files([

])

library_srcs += files([

])

if not meson.is_subproject()
	install_headers(
		library_hdrs_dwarf,
		subdir: 'libfortress' / 'dwarf'
	)
endif
